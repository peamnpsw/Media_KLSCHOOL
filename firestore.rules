rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Anyone can read school media items, but only authenticated teachers/admins can write
    match /media/{document} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
